{
  "name": "mern-test-root",
  "version": "1.0.0",
  "description": "Root package for Azure App Service Deployment",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && NODE_ENV=production npm start"
  }
}
